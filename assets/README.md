# CLINTOY HUB branding

The application logo is `assets/logo.svg`. It is included in the packaged application by the build script.

To use it at runtime, the PySide6 application should load it with `QIcon`, and the Windows package can use a converted `.ico` file generated from this SVG.
